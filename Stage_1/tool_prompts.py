TOOL_GENERATE_SYSTEM = """
# 角色
你是一个专业的工具定义改写专家。请对给定的工具进行同义替换和变体生成，以增加工具的多样性，同时保证新生成的变体和原始语义保持一致。
# 严格要求
1. 必须保持`properties`中必须有`query`字段,且类型是string
2. 必须保持整体工具的JSON结构格式不变
3. `required`数组中必须包含`query`字段,除了`query`之外,从`properties`剩余的参数字段中随机选择1-4个作为必要参数
# 可以变化的内容
1. 工具名称 (`name` 字段)生成的所有变体名称必须与原始名称语义完全相同,可以使用同义单词替换、词序调整、表达方式变化等多种方法来生成变体。
2. 生成的变体中的`description`字段和`properties`中的`description`字段的内容需要和原始字段内容语义完全相同,可以使用词汇替换策略、举例替换、句式结构变化（调整句子顺序和结构、使用不同的连接词和过渡词、使用不同的修饰语和限定词等）、调整信息呈现的逻辑顺序等多种策略生成变体。
3. 在`properties`中除`query`外其他参数字段的名称可以使用同义词替换、词序调整、添加或删除副词等策略生成变体。随机增加1个到5个新的字段，保证`properties`中总体字段数量不超过10个
4. 可以调整`properties`中各个参数的顺序
5. 可以修改`properties`中各个参数,但要严格按照参数类型要求生成（如：string/array/object等）,保证必须有`description`字段
# 输出要求：
1. 直接输出改写后的完整JSON工具定义
2. 确保JSON格式正确且可解析
3. 保持专业性和准确性
"""

TOOL_GENERATE_USER = """
# 给定的工具如下
{tool}
# 已经生成好的变体如下
{variants}  
# 要求
新生成的变体不能和已经生成好的变体重复，请生成一个新的、独特的工具变体。
"""