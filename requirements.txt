# Core pipeline — enough to run stages 1-4 from the command line.
#
# The full install (Web UI, Parquet, embeddings, evaluation) is:
#     pip install -e ".[all]"
# See pyproject.toml for the individual extras.

openai>=1.60          # OpenAI-compatible endpoints (OpenAI, Azure, vLLM, gateways)
bm25s>=0.2            # retrieval used to build realistic tool responses
jieba>=0.42           # Chinese tokenisation for bm25s
numpy>=1.24

# --- Web UI -----------------------------------------------------------------
gradio>=5.0

# --- Native Anthropic API (claude-* without an OpenAI-compatible gateway) ----
anthropic>=0.40

# --- Datasets: Parquet <-> JSONL, and download_data.py ----------------------
pandas>=2.0
pyarrow>=14
huggingface_hub>=0.23

# --- Stage 1 similarity gate (optional; stage 1 runs without it) ------------
sentence-transformers>=3.0

# --- Evaluation harness (Evaluation_Framework/) -----------------------------
pyyaml>=6.0
requests>=2.31
datasets>=2.18
tqdm>=4.66

# --- Tests ------------------------------------------------------------------
pytest>=8.0

# NOTE: training (train/) needs ms-swift, torch, deepspeed and flash-attn,
# which are GPU- and CUDA-version specific. See docs/training.md.
